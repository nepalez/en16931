//! End-to-end scenario tests across the core and the extensions.
//!
//! The crate carries no code of its own. Its integration tests compose the core
//! with the extension crates, which the core itself must never depend on.

pub mod prelude;
