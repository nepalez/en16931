[package]
name = "en16931-phive"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Wrapper extension for the phive validator envelope"

[dependencies]
en16931-core = { path = "../../core", version = "0.1" }
